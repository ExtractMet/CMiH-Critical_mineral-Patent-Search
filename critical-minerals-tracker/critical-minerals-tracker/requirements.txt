# Minimal on purpose. Streamlit Community Cloud already ships prebuilt
# numpy / pandas / pyarrow wheels for its Python (currently 3.13); pinning older
# versions of those forces slow source-compiles on deploy. np_compat.py handles
# the NumPy 2.0 `np.unicode_` compatibility at runtime, so nothing needs pinning.
streamlit>=1.40
pandas
plotly>=5.18
requests
